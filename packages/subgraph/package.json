{
  "name": "subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen --output-dir ./generated",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ znft-goerli",
    "create-local": "graph create --node http://localhost:8020/ znft-goerli",
    "remove-local": "graph remove --node http://localhost:8020/ znft-goerli",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 znft-goerli",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
