{
  "name": "znft-website",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "site/*"
    ]
  },
  "scripts": {
    "build": "turbo run build --scope=marketplace --include-dependencies --no-deps",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --scope=marketplace --include-dependencies --no-deps",
    "start:node": "yarn workspace contract start:node",
    "dev:deploy": "yarn workspace contract deploy:local",
    "test:deploy": "yarn workspace contract deploy:rinkeby",
    "start": "turbo run start",
    "types": "turbo run types",
    "prettier-fix": "prettier --write ."
  },
  "devDependencies": {
    "husky": "8.0.1",
    "lint-staged": "12.5.0",
    "prettier": "2.7.1",
    "turbo": "1.4.7"
  },
  "husky": {
    "hooks": {
      "pre-commit": "turbo run lint"
    }
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx}": [
      "eslint",
      "prettier --write",
      "git add"
    ],
    "**/*.{md,mdx,json}": [
      "prettier --write",
      "git add"
    ]
  },
  "packageManager": "yarn@3.2.1",
  "installConfig": {
    "hoistingLimits": "**/@nomiclabs/**"
  }
}
